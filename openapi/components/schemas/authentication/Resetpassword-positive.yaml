type: object
properties:
  data: 
    type: object
    properties:
      old-password:
        description: Antigua Contraseña
        type: string
        required: true
        example: antiguacontra
      new-password:
        description: Nueva Contraseña
        type: string
        required: true
        example: nuevacontra
  message:
    type: string
    description: Mensaje positivo de respuesta
    example: Hola, se pudo cambiar la contraseña
  status:
    type: integer
    description: Estado positivo de la respuesta
    example: 200