type: object
properties:
  data: 
    type: object
    properties:
      type_user:
        description: Tipo de usuario (vendor, store_api)
        type: string
        required: true
        example: vendor_user
      user:
        $ref: ../User2.yaml
  message:
    type: string
    description: Mensaje positivo de respuesta
    example: Hola, se obtuvieron los permisos
  status:
    type: integer
    description: Estado positivo de la respuesta
    example: 200