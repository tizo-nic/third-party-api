type: object
properties:
  data: 
    type: object
    properties:
      typeUser:
        description: Tipo de usuario (vendor, store_api)
        type: string
        required: true
        example: vendor_user
      User:
        $ref: ../User2.yaml
  message:
    type: string
    description: Mensaje positive de respuesta
    example: Hola, se obtuvieron los permisos
  status:
    type: integer
    description: Estado positive de la respuesta
    example: 200