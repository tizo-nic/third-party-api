type: object
properties:
  data: 
    type: object
    properties:
      payments:
        type: array
        items:
          $ref:  "./payments.yaml"
      count: 
        type: integer
        description: Número de los pagos disponibles
        example: 3
  message:
    type: string
    description: Mensaje positivo de respuesta
    example: Lista de tipos de pagos disponibles encontrados
  status:
    type: integer
    description: Estado positivo de la respuesta
    example: 200